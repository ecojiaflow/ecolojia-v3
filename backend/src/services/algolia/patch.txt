      // Options de recherche
      const searchOptions = {
        page: options.page || 0,
        hitsPerPage: options.hitsPerPage || 20,
        facets: ['category', 'brand', 'nova', 'nutriscore', 'ecoscore', 'tags'],
        maxValuesPerFacet: 50,
        // Ajouter directement facetFilters et numericFilters s'ils sont présents
        ...(options.facetFilters && { facetFilters: options.facetFilters }),
        ...(options.numericFilters && { numericFilters: options.numericFilters })
      };
